# Pointer

The canonical project guide lives at the repo root: `../CLAUDE.md`. It is loaded automatically — this file intentionally left minimal to avoid duplicating its full content into every session's context.
